apiVersion: v1
kind: Service
metadata:
  name: skillexchange-app-service
  namespace: cseb-mid-ns
spec:
  selector:
    app: skillexchange-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30081
  type: NodePort